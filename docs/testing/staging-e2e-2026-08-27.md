# Staging E2E — 2026-08-27

Status: przygotowane do ręcznego checkpointu HITL; nie wykonano bez potwierdzenia
użytkownika i danych testowego konta.

## Checkpoint infrastruktury

Odczyt historii migracji stagingu wykonano 2026-08-27. Historia nie jest zgodna
z repozytorium: staging zawiera wersje `20260824065547`, `20260824065557`,
`20260824065614`, `20260825130018`, których nie ma lokalnie; lokalne wersje
`20260809130312`, `20260823070000`, `20260824054519` i `20260825071833` nie są
oznaczone jako zastosowane na stagingu. `pnpm supabase:plan` zatrzymał się na
`Remote migration versions not found in local migrations directory`.

Nie wykonano `migration repair`, `db pull` ani `db push`. Przed E2E właściciel
powinien wyjaśnić drift na osobnej gałęzi/backupie i ponownie uruchomić dry-run.

Zdalny lint Supabase wykonano 2026-08-27: zakończył się bez błędu, ale zwrócił
ostrzeżenia o rzutowaniu `text` → `uuid[]` w `update_goal_details` i
`update_knowledge_item` oraz nieużywanej zmiennej/parametrze w
`create_knowledge_with_relations`. Nie są częścią tej zmiany, ale należy je
przejrzeć przed produkcją.

## Scenariusz

| Krok | Oczekiwany wynik | Wynik |
| --- | --- | --- |
| Rejestracja nowego konta | Konto i pusty Workspace bez danych demo | HITL |
| Logowanie | Otwiera się pusty Start | HITL |
| Capture tekstu | Element trafia do Wiedza → Skrzynka | HITL |
| Triage jako Działanie | Działanie jest przypięte i następuje przejście na Start | HITL |
| Ukończenie | Pierwsze Działanie ma status ukończone | HITL |
| Reload | Stan pozostaje po odświeżeniu | HITL |
| Drugi browser | Ten sam Workspace widzi zapis i ukończenie | HITL |
| Eksport | Pobiera się JSON bez błędu | HITL |
| Ponowne logowanie | Dane są widoczne po wylogowaniu i zalogowaniu | HITL |
| Błąd testowy | Monitoring dostaje bezpieczny payload bez treści użytkownika | HITL |

## Dane wykonania

- Staging project ref: uzupełnić po checkpointcie, nie wpisywać klucza.
- Konto testowe: utworzyć jednorazowo w stagingu; nie wpisywać hasła do repo.
- Frontend commit/deployment: uzupełnić po uruchomieniu.
- Monitoring event ID: uzupełnić po błędzie testowym.
- Eksport: potwierdzić pobranie i usunięcie lokalnego artefaktu po weryfikacji.

Po przejściu wszystkich wierszy zmienić status na `PASS`, wpisać datę/czas,
przeglądarki i krótkie obserwacje. Do czasu takiego wpisu nie wdrażać na
produkcję.
